<p id="notice"><%= notice %></p>

<h1>Vehicles</h1>

<%= link_to 'New Vehicle', new_vehicle_path %>

<table>
  <thead>
    <tr>
      <th>License plate</th>
      <th>Type</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @vehicles.each do |vehicle| %>
      <tr>
        <td><%= vehicle.license_plate %></td>
        <td><%= vehicle.vehicle_type %></td>
        <td><%= link_to 'Show last visit', vehicle %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>
