<%= form_with(model: vehicle, local: true) do |form| %>
  <% if vehicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vehicle.errors.count, "error") %> prohibited this vehicle from being saved:</h2>

      <ul>
      <% vehicle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :vehicle_type %>
    <%= form.text_field :vehicle_type, id: :vehicle_type %>
  </div>

  <div class="field">
    <%= form.label :license_plate %>
    <%= form.text_field :license_plate, id: :vehicle_license_plate %>
  </div>

  <div class="field">
    <%= form.label :muddy_bed %>
    <%= form.check_box :muddy_bed, id: :vehicle_muddy_bed %>
  </div>

  <div class="field">
    <%= form.label :tailgate_down %>
    <%= form.check_box :tailgate_down, id: :vehicle_tailgate_down %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
